schema: "${GERALT_ENDPOINT:http://localhost:4000}/graphql"
generates:
  ./src/model/schema.graphql:
    config:
      content: |
        # This schema was generated automatically by `pnpm generate` command
        # from GraphQL API schema (${GERALT_ENDPOINT:http://localhost:4000}/graphql).
        #
        # **DO NOT EDIT THIS FILE MANUALLY**
    plugins:
      - add
      - schema-ast
    hooks:
      pOneFileWrite:
        # - pnpm prettier --write
