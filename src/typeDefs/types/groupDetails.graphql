type GroupDetails {
  id: String!
  title: String!
  description: String!
  createdAt: Date!
  sponsored: Boolean!
  verified: Boolean!
  remote: Boolean!
  cities: [City!]!
  categories: [Category!]!
  members: [GroupUser!]!
  comments: [Comment!]!
  upcoming: [GroupedEvents!]!
  pending: [GroupedEvents!]!
  past: [GroupedEvents!]!
  cancelled: [GroupedEvents!]!
  events: [EventGroup!]!
  upcomingLength: Int!
  pendingLength: Int
  pastLength: Int!
  cancelledLength: Int!
  eventsLength: Int!
  rate: Float!
  smallPhoto: String!
  mediumPhoto: String!
  largePhoto: String!
}
